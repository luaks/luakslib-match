{
  "name": "@luaks/match",
  "version": "1.0.1",
  "description": "A small library to provide pattern matching functionality.",
  "main": "match.js",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "clean": "rm -rf package *.js",
    "prepare-package": "npm run clean && npm run build && mkdir -p package && cp match.js match.d.ts package.json package"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/luaks/luakslib-match.git"
  },
  "keywords": [
    "typescript"
  ],
  "author": "luaks",
  "license": "ISC",
  "homepage": "https://github.com/luaks/luakslib-match#readme",
  "devDependencies": {
    "@types/jest": "^26.0.22",
    "jest": "^26.6.3",
    "ts-jest": "^26.5.4",
    "typescript": "^4.2.3"
  }
}
